import { keyframes } from "@stitches/react";

const scrollDownKeyframes = keyframes({
  "0%": { transform: "translateY(0)" },
  "100%": { transform: "translateY(4px)" },
});

export default scrollDownKeyframes;
