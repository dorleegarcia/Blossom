import Icon from "../Icon";

import StyledScrollDown from "./styles";

function ScrollDown(): React.ReactElement {
  return (
    <StyledScrollDown>
      <Icon icon="scroll_down" size={15} />
    </StyledScrollDown>
  );
}

export default ScrollDown;
